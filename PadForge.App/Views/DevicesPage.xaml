<UserControl x:Class="PadForge.Views.DevicesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:common="clr-namespace:PadForge.Common"
             xmlns:local="clr-namespace:PadForge.Views"
             xmlns:strings="clr-namespace:PadForge.Resources.Strings">

    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE772;" FontFamily="Segoe MDL2 Assets" FontSize="20"
                       VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Devices_Title, Source={x:Static strings:Strings.Instance}}" Style="{StaticResource TitleTextBlockStyle}"/>
            <Button Content="{Binding Common_Refresh, Source={x:Static strings:Strings.Instance}}" Margin="16,0,0,0"
                    Command="{Binding RefreshCommand}"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                <TextBlock Opacity="0.6" Text="{Binding OnlineCount, Mode=OneWay}"/>
                <TextBlock Opacity="0.6" Text="{Binding Devices_OnlineTotal, Source={x:Static strings:Strings.Instance}}"/>
                <TextBlock Opacity="0.6" Text="{Binding TotalCount, Mode=OneWay}"/>
                <TextBlock Opacity="0.6" Text="{Binding Devices_Total, Source={x:Static strings:Strings.Instance}}"/>
            </StackPanel>
        </StackPanel>

        <!-- Main content: Card list + Detail -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="340"/>
            </Grid.ColumnDefinitions>

            <!-- ═══════════════════════════════════════════ -->
            <!--  Left: Device card list                    -->
            <!-- ═══════════════════════════════════════════ -->
            <ListBox ItemsSource="{Binding Devices}"
                     SelectedItem="{Binding SelectedDevice, Mode=TwoWay}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     BorderThickness="0" Background="Transparent"
                     Margin="0,0,12,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="12,10" Margin="0,0,0,4"
                                PreviewMouseLeftButtonDown="DeviceCard_MouseDown"
                                PreviewMouseMove="DeviceCard_MouseMove"
                                PreviewMouseLeftButtonUp="DeviceCard_MouseUp">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 0: Status dot + Device name -->
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8"
                                             Fill="{Binding IsOnline, Converter={StaticResource BoolToColorConverter}, ConverterParameter=gray}"
                                             VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding DeviceName}" FontWeight="SemiBold"
                                               FontSize="13" TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"/>
                                </StackPanel>

                                <!-- Slot badges (right, row 0–1, wraps if many) -->
                                <StackPanel Grid.Column="1" Grid.RowSpan="2"
                                            VerticalAlignment="Center" Margin="8,0,0,0">
                                    <ItemsControl ItemsSource="{Binding SlotBadges}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="4" Padding="5,2"
                                                        Margin="0,0,4,3"
                                                        Background="{DynamicResource SubtleFillColorSecondaryBrush}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="&#xE7FC;" FontFamily="Segoe MDL2 Assets"
                                                                   FontSize="10" Opacity="0.6"
                                                                   VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding SlotNumber}" FontSize="10"
                                                                   Opacity="0.7" Margin="3,0,0,0"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <!-- Unassigned fallback -->
                                    <Border CornerRadius="4" Padding="5,2" Margin="0,0,4,3"
                                            Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                            Visibility="{Binding IsUnassigned, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="{Binding Common_Unassigned, Source={x:Static strings:Strings.Instance}}" FontSize="10" Opacity="0.55"/>
                                    </Border>
                                </StackPanel>

                                <!-- Delete button (upper-right) -->
                                <Button Grid.Column="2" Grid.RowSpan="2"
                                        ToolTip="{Binding Devices_RemoveTooltip, Source={x:Static strings:Strings.Instance}}"
                                        Background="Transparent" Padding="2" MinWidth="0" MinHeight="0"
                                        VerticalAlignment="Top" Margin="4,0,0,0" Opacity="0.5"
                                        Click="RemoveDevice_Click">
                                    <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="11"/>
                                </Button>

                                <!-- Row 1: Type | VID:PID | Capabilities -->
                                <StackPanel Grid.Row="1" Grid.ColumnSpan="3"
                                            Orientation="Horizontal" Margin="16,3,0,0">
                                    <TextBlock Text="{Binding DeviceType}" FontSize="11" Opacity="0.55"/>
                                    <TextBlock Text=" &#x2022; " FontSize="11" Opacity="0.3"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontSize="11" Opacity="0.45" Text="{Binding VendorIdHex, Mode=OneWay}"/>
                                        <TextBlock FontSize="11" Opacity="0.45" Text=":"/>
                                        <TextBlock FontSize="11" Opacity="0.45" Text="{Binding ProductIdHex, Mode=OneWay}"/>
                                    </StackPanel>
                                    <TextBlock Text=" &#x2022; " FontSize="11" Opacity="0.3"/>
                                    <TextBlock Text="{Binding CapabilitiesSummary}" FontSize="11" Opacity="0.45"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Grid>
                                        <!-- Selection accent bar (left edge) -->
                                        <Border x:Name="SelectionBar" Width="4"
                                                HorizontalAlignment="Left" CornerRadius="2"
                                                Background="{DynamicResource SystemAccentColorSecondaryBrush}"
                                                Visibility="Collapsed" Margin="0,2"/>
                                        <ContentPresenter Margin="6,0,0,0"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="SelectionBar" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <!-- ═══════════════════════════════════════════ -->
            <!--  Right: Detail panel                       -->
            <!-- ═══════════════════════════════════════════ -->
            <Border Grid.Column="1"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8" Padding="14,12">
                <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled"
                              Visibility="{Binding HasSelectedDevice, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>

                        <!-- Device identity -->
                        <TextBlock Text="{Binding SelectedDevice.DeviceName}"
                                   FontSize="16" FontWeight="SemiBold"
                                   TextWrapping="Wrap" Margin="0,0,0,6"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,1">
                            <TextBlock FontSize="11" Opacity="0.7" Text="{Binding Devices_Product, Source={x:Static strings:Strings.Instance}}"/>
                            <TextBlock FontSize="11" Opacity="0.7" Text="{Binding SelectedDevice.ProductName, Mode=OneWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,1">
                            <TextBlock FontSize="11" Opacity="0.7" Text="{Binding Devices_Type, Source={x:Static strings:Strings.Instance}}"/>
                            <TextBlock FontSize="11" Opacity="0.7" Text="{Binding SelectedDevice.DeviceType, Mode=OneWay}"/>
                        </StackPanel>
                        <TextBlock FontSize="11" Opacity="0.6" Margin="0,0,0,8"
                                   Text="{Binding SelectedDevice.CapabilitiesSummary, Mode=OneWay}"/>

                        <Border ClipToBounds="True" Margin="0,0,0,1" Padding="0,1">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="11" Opacity="0.6"
                                           common:MarqueeBehavior.IsEnabled="True">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}{1}">
                                            <Binding Path="Devices_AppGUID" Source="{x:Static strings:Strings.Instance}"/>
                                            <Binding Path="SelectedDevice.InstanceGuid" Mode="OneWay"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                        <Border ClipToBounds="True" Margin="0,0,0,1" Padding="0,1"
                                Visibility="{Binding SelectedDevice.SdlGuid, Converter={StaticResource StringToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="11" Opacity="0.6"
                                           common:MarqueeBehavior.IsEnabled="True">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}{1}">
                                            <Binding Path="Devices_SdlGUID" Source="{x:Static strings:Strings.Instance}"/>
                                            <Binding Path="SelectedDevice.SdlGuid" Mode="OneWay"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                        <Border ClipToBounds="True" Margin="0,0,0,1" Padding="0,1"
                                Visibility="{Binding SelectedDevice.HidHideInstancePath, Converter={StaticResource StringToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="11" Opacity="0.6"
                                           common:MarqueeBehavior.IsEnabled="True">
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}{1}">
                                            <Binding Path="Devices_InstancePath" Source="{x:Static strings:Strings.Instance}"/>
                                            <Binding Path="SelectedDevice.HidHideInstancePath" Mode="OneWay"/>
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock FontSize="11" Opacity="0.6" Text="{Binding Devices_VIDPID, Source={x:Static strings:Strings.Instance}}"/>
                            <TextBlock FontSize="11" Opacity="0.6" Text="{Binding SelectedDevice.VendorIdHex, Mode=OneWay}"/>
                            <TextBlock FontSize="11" Opacity="0.6" Text=":"/>
                            <TextBlock FontSize="11" Opacity="0.6" Text="{Binding SelectedDevice.ProductIdHex, Mode=OneWay}"/>
                        </StackPanel>

                        <!-- Submit mapping (joysticks only — not gamepads, mice, or keyboards) -->
                        <Button Content="{Binding Devices_SubmitMapping, Source={x:Static strings:Strings.Instance}}" Margin="0,6,0,8"
                                Padding="8,4" HorizontalAlignment="Left"
                                Click="SubmitMapping_Click"
                                Visibility="{Binding SelectedDevice.ShowSubmitMapping, Converter={StaticResource BoolToVisibilityConverter}}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"/>
                            </Button.Style>
                        </Button>

                        <Separator Margin="0,2,0,6"/>

                        <!-- Assign to slot -->
                        <TextBlock Text="{Binding Devices_VCAssignment, Source={x:Static strings:Strings.Instance}}" FontWeight="SemiBold"
                                   FontSize="13" Margin="0,0,0,6"/>
                        <WrapPanel Margin="0,0,0,6">
                            <ItemsControl ItemsSource="{Binding ActiveSlotItems}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ToggleButton IsChecked="{Binding IsAssigned, Mode=OneWay}"
                                                      Command="{Binding DataContext.ToggleSlotCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                      CommandParameter="{Binding PadIndex}"
                                                      Margin="0,0,3,3" Padding="5,3"
                                                      ToolTip="{Binding Devices_ToggleAssignment, Source={x:Static strings:Strings.Instance}}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE7FC;" FontFamily="Segoe MDL2 Assets"
                                                           FontSize="10" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding SlotNumber}" FontSize="10"
                                                           VerticalAlignment="Center" Margin="3,0,0,0"/>
                                            </StackPanel>
                                        </ToggleButton>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </WrapPanel>

                        <Separator Margin="0,2,0,8"
                                   Visibility="{Binding SelectedDevice.ShowInputModeOrHidingSection, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <!-- ─── Input Mode (real gamepads only — virtual web/overlay sources skip) ─── -->
                        <StackPanel Visibility="{Binding SelectedDevice.ShowInputModeSection, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{Binding Devices_InputMode, Source={x:Static strings:Strings.Instance}}" FontWeight="SemiBold"
                                       FontSize="13" Margin="0,0,0,6"/>

                            <CheckBox Content="{Binding Devices_ForceRaw, Source={x:Static strings:Strings.Instance}}"
                                      IsChecked="{Binding SelectedDevice.ForceRawJoystickMode, Mode=TwoWay}"
                                      Checked="HidingToggle_Changed" Unchecked="HidingToggle_Changed"
                                      ToolTip="{Binding Devices_ForceRawTooltip, Source={x:Static strings:Strings.Instance}}"
                                      Margin="0,0,0,4"/>

                            <Separator Margin="0,2,0,8"/>
                        </StackPanel>

                        <!-- ─── Input Hiding ─── -->
                        <TextBlock Text="{Binding Devices_InputHiding, Source={x:Static strings:Strings.Instance}}" FontWeight="SemiBold"
                                   FontSize="13" Margin="0,0,0,6"
                                   Visibility="{Binding SelectedDevice.ShowInputHidingSection, Converter={StaticResource BoolToVisibilityConverter}}"/>

                        <CheckBox Content="{Binding Devices_HideFromGames, Source={x:Static strings:Strings.Instance}}"
                                  IsChecked="{Binding SelectedDevice.HidHideEnabled, Mode=TwoWay}"
                                  IsEnabled="{Binding SelectedDevice.IsHidHideAvailable}"
                                  Visibility="{Binding SelectedDevice.IsInternalVirtual, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}"
                                  Checked="HidingToggle_Changed" Unchecked="HidingToggle_Changed"
                                  Margin="0,0,0,4">
                            <CheckBox.ToolTip>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding Devices_HideTooltip, Source={x:Static strings:Strings.Instance}}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedDevice.IsHidHideAvailable}" Value="False">
                                                    <Setter Property="Text" Value="{Binding Devices_HideTooltipInstall, Source={x:Static strings:Strings.Instance}}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </CheckBox.ToolTip>
                        </CheckBox>

                        <CheckBox Content="{Binding Devices_ConsumeMapped, Source={x:Static strings:Strings.Instance}}"
                                  IsChecked="{Binding SelectedDevice.ConsumeInputEnabled, Mode=TwoWay}"
                                  Checked="HidingToggle_Changed" Unchecked="HidingToggle_Changed"
                                  ToolTip="{Binding Devices_ConsumeTooltip, Source={x:Static strings:Strings.Instance}}"
                                  Margin="0,0,0,4">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedDevice.ShowConsumeToggle}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>

                        <Separator Margin="0,4,0,8"/>

                        <!-- ─── Raw Input State (Visual) ─── -->
                        <TextBlock Text="{Binding Devices_RawInputState, Source={x:Static strings:Strings.Instance}}" FontWeight="SemiBold"
                                   FontSize="13" Margin="0,0,0,8"/>

                        <!-- Axes label (joysticks/gamepads only) -->
                        <TextBlock Text="{Binding Devices_Axes, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="{Binding HasRawData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsTouchpadDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMidiDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <!-- Axes: progress bars (joysticks/gamepads only) -->
                        <ItemsControl ItemsSource="{Binding RawAxes}" Margin="0,0,0,8">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsTouchpadDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMidiDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="48"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="44"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" FontSize="10" Opacity="0.55"
                                                   VerticalAlignment="Center"/>
                                        <ProgressBar Grid.Column="1" Minimum="0" Maximum="1"
                                                     Value="{Binding NormalizedValue, Mode=OneWay}"
                                                     Height="8" Margin="4,0"/>
                                        <TextBlock Grid.Column="2" FontSize="9" Opacity="0.45"
                                                   FontFamily="Consolas" VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"
                                                   Text="{Binding RawValue}"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Buttons label and grid: shown for any device that exposes
                             button slots (gamepads, joysticks, touchpads with a
                             click button). Keyboards / mice have their own
                             dedicated visual previews instead. -->
                        <TextBlock Text="{Binding Devices_Buttons, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="{Binding HasRawData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMidiDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding RawButtons}" Margin="0,0,0,8">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsMidiDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="24" Height="24" CornerRadius="12" Margin="1"
                                            ToolTip="{Binding DisplayNumber, StringFormat='Button {0}'}">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#28888888"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPressed}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding DisplayNumber}" FontSize="8"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Opacity="0.7"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Keyboard: visual QWERTY layout -->
                        <TextBlock Text="{Binding Devices_Keyboard, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Viewbox Stretch="Uniform" HorizontalAlignment="Left" Margin="0,0,0,8"
                                 StretchDirection="DownOnly">
                            <Viewbox.Style>
                                <Style TargetType="Viewbox">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsKeyboardDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Viewbox.Style>
                            <ItemsControl ItemsSource="{Binding KeyboardKeys}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas Width="556" Height="136"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="{Binding KeyWidth}" Height="{Binding KeyHeight}"
                                                CornerRadius="3"
                                                ToolTip="{Binding Label}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#28888888"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPressed}" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Label}" FontSize="8"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Opacity="0.8" TextTrimming="None"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Viewbox>

                        <!-- Mouse: visual mouse graphic -->
                        <TextBlock Text="{Binding Devices_Mouse, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Viewbox Stretch="Uniform" HorizontalAlignment="Left" Margin="0,0,0,8"
                                 StretchDirection="DownOnly" MaxHeight="280">
                            <Viewbox.Style>
                                <Style TargetType="Viewbox">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Viewbox.Style>
                            <local:MousePreviewControl/>
                        </Viewbox>

                        <!-- MIDI: live piano (wrapped, all 128 notes) + CC display -->
                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsMidiDevice}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <!-- Notes -->
                            <TextBlock Text="{Binding Devices_MidiNotes, Source={x:Static strings:Strings.Instance}}"
                                       FontSize="11" Opacity="0.6" Margin="0,0,0,3"/>
                            <ItemsControl ItemsSource="{Binding MidiNotes}" Margin="0,0,0,8">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="15" Height="38" CornerRadius="0,0,2,2" Margin="0"
                                                BorderBrush="{DynamicResource SolidBackgroundFillColorBaseBrush}" BorderThickness="1"
                                                ToolTip="{Binding Label}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <!-- White key default -->
                                                    <Setter Property="Background" Value="#E8E8E8"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsBlackKey}" Value="True">
                                                            <Setter Property="Background" Value="#202020"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsOn}" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource SystemAccentColorSecondaryBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding OctaveMarker}" FontSize="7"
                                                       Foreground="#808080"
                                                       HorizontalAlignment="Center" VerticalAlignment="Bottom"
                                                       Margin="0,0,0,1"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Pitch bend -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="{Binding Mapping_MidiPitchBend, Source={x:Static strings:Strings.Instance}}"
                                           FontSize="11" Opacity="0.6" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <Slider Minimum="-1" Maximum="1" Value="{Binding MidiPitchBend, Mode=OneWay}"
                                        Width="160" IsEnabled="False" VerticalAlignment="Center"/>
                            </StackPanel>

                            <!-- Controllers (CC) -->
                            <TextBlock Text="{Binding Devices_MidiControllers, Source={x:Static strings:Strings.Instance}}"
                                       FontSize="11" Opacity="0.6" Margin="0,0,0,3"/>
                            <ItemsControl ItemsSource="{Binding MidiCcs}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="40" Height="34" Margin="0,0,3,3" CornerRadius="3"
                                                Background="{DynamicResource SubtleFillColorSecondaryBrush}"
                                                ToolTip="{Binding Label}">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Opacity" Value="0.45"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                            <Setter Property="Opacity" Value="1"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <ProgressBar Grid.Row="0" Minimum="0" Maximum="1"
                                                             Value="{Binding Normalized, Mode=OneWay}"
                                                             Margin="3,3,3,1" Height="6" VerticalAlignment="Top"/>
                                                <TextBlock Grid.Row="1" Text="{Binding Label}" FontSize="8"
                                                           HorizontalAlignment="Center" Opacity="0.8" Margin="0,0,0,2"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- POV Hats: compass indicators -->
                        <TextBlock Text="{Binding Devices_DPadPOV, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6"
                                   Margin="0,0,0,3">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding RawPovs.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding RawPovs}" Margin="0,0,0,8">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                        <Canvas Width="36" Height="36">
                                            <!-- Background circle -->
                                            <Ellipse Width="36" Height="36"
                                                     Fill="#18888888" Stroke="#30888888"
                                                     StrokeThickness="1"/>
                                            <!-- Center dot -->
                                            <Ellipse Width="4" Height="4" Canvas.Left="16" Canvas.Top="16"
                                                     Fill="#50888888"/>
                                            <!-- Direction indicator line -->
                                            <Line X1="18" Y1="18" X2="18" Y2="5"
                                                  Stroke="{DynamicResource SystemAccentColorSecondaryBrush}"
                                                  StrokeThickness="2.5" StrokeEndLineCap="Round">
                                                <Line.Style>
                                                    <Style TargetType="Line">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCentered}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Line.Style>
                                                <Line.RenderTransform>
                                                    <RotateTransform Angle="{Binding AngleDegrees}"
                                                                     CenterX="18" CenterY="18"/>
                                                </Line.RenderTransform>
                                            </Line>
                                        </Canvas>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                                            <TextBlock FontSize="10" Opacity="0.5" Text="{Binding Devices_POV, Source={x:Static strings:Strings.Instance}}"/>
                                            <TextBlock FontSize="10" Opacity="0.5" Text="{Binding Index, Mode=OneWay}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Gyroscope -->
                        <TextBlock Text="{Binding Devices_Gyroscope, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3"
                                   Visibility="{Binding HasGyroData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Border Visibility="{Binding HasGyroData, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="#10888888" CornerRadius="6" Padding="10,6" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="X" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding GyroX, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Y" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding GyroY, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Z" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding GyroZ, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Accelerometer -->
                        <TextBlock Text="{Binding Devices_Accelerometer, Source={x:Static strings:Strings.Instance}}" FontSize="11" Opacity="0.6" Margin="0,0,0,3"
                                   Visibility="{Binding HasAccelData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Border Visibility="{Binding HasAccelData, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="#10888888" CornerRadius="6" Padding="10,6" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="X" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding AccelX, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Y" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding AccelY, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Z" FontSize="10" Opacity="0.5" HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="11" FontFamily="Consolas" HorizontalAlignment="Center"
                                               Text="{Binding AccelZ, StringFormat='{}{0:F3}'}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Touchpad preview -->
                        <TextBlock Text="{Binding TouchpadLabel}"
                                   FontSize="11" Opacity="0.6" Margin="0,0,0,3"
                                   Visibility="{Binding HasTouchpadData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Border x:Name="TouchpadPreviewBorder"
                                Visibility="{Binding HasTouchpadData, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="#10888888"
                                CornerRadius="6" Margin="0,0,0,8"
                                Height="100" ClipToBounds="True">
                            <Canvas>
                                <!-- Up to 5 simultaneous touchpad contacts.
                                     SDL gamepad pads typically expose 1-2;
                                     Windows PTP can deliver up to 5. Each
                                     dot's visibility binds to its own
                                     TouchpadDownN flag so unused slots
                                     stay hidden on devices that don't
                                     report that many contacts. -->
                                <Ellipse x:Name="TouchpadDot0" Width="14" Height="14" Fill="#CCFF6600"
                                         Visibility="{Binding TouchpadDown0, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="TouchpadDot1" Width="14" Height="14" Fill="#CC0066FF"
                                         Visibility="{Binding TouchpadDown1, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="TouchpadDot2" Width="14" Height="14" Fill="#CC4CAF50"
                                         Visibility="{Binding TouchpadDown2, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="TouchpadDot3" Width="14" Height="14" Fill="#CCBA68C8"
                                         Visibility="{Binding TouchpadDown3, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="TouchpadDot4" Width="14" Height="14" Fill="#CCFFEB3B"
                                         Visibility="{Binding TouchpadDown4, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </Canvas>
                        </Border>

                        <!-- Second touchpad preview — shown only for multi-pad devices
                             (Steam Controller 2026 / Steam Deck / original Steam Controller). -->
                        <TextBlock Text="{Binding Touchpad2Label}"
                                   FontSize="11" Opacity="0.6" Margin="0,0,0,3"
                                   Visibility="{Binding HasSecondTouchpadData, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <Border x:Name="Touchpad2PreviewBorder"
                                Visibility="{Binding HasSecondTouchpadData, Converter={StaticResource BoolToVisibilityConverter}}"
                                Background="#10888888"
                                CornerRadius="6" Margin="0,0,0,8"
                                Height="100" ClipToBounds="True">
                            <Canvas>
                                <Ellipse x:Name="Pad2Dot0" Width="14" Height="14" Fill="#CCFF6600"
                                         Visibility="{Binding Pad2Down0, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="Pad2Dot1" Width="14" Height="14" Fill="#CC0066FF"
                                         Visibility="{Binding Pad2Down1, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="Pad2Dot2" Width="14" Height="14" Fill="#CC4CAF50"
                                         Visibility="{Binding Pad2Down2, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="Pad2Dot3" Width="14" Height="14" Fill="#CCBA68C8"
                                         Visibility="{Binding Pad2Down3, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <Ellipse x:Name="Pad2Dot4" Width="14" Height="14" Fill="#CCFFEB3B"
                                         Visibility="{Binding Pad2Down4, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </Canvas>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
